const days=Object.keys(timetable);let currentDayIndex=new Date().getDay()-1;if(currentDayIndex<0 || currentDayIndex>=days.length)currentDayIndex=0;function loadTimetable(){const saved=localStorage.getItem('customTimetable');if(saved){try{return JSON.parse(saved);}catch(e){console.log('Failed to parse saved timetable,using default');return timetable;}}return timetable;}let currentTimetable=loadTimetable();function saveTimetable(){localStorage.setItem('customTimetable',JSON.stringify(currentTimetable));}function resetTimetable(){if(confirm('Are you sure you want to reset the timetable to default?')){localStorage.removeItem('customTimetable');currentTimetable=JSON.parse(JSON.stringify(timetable));updateEditForm();renderDay();}}function toggleEditMode(){document.getElementById('editModal').classList.remove('hidden');document.getElementById('editDay').value=days[currentDayIndex];updateEditForm();}function closeEditModal(){document.getElementById('editModal').classList.add('hidden');}function showAboutInfo(){document.getElementById('aboutModal').classList.remove('hidden');}function closeAboutModal(){document.getElementById('aboutModal').classList.add('hidden');}window.addEventListener('click',function(event){const editModal=document.getElementById('editModal');const aboutModal=document.getElementById('aboutModal');if(event.target===editModal){closeEditModal();}if(event.target===aboutModal){closeAboutModal();}});function updateEditForm(){const selectedDay=document.getElementById('editDay').value;const periods=currentTimetable[selectedDay];const periodsList=document.getElementById('periodsList');periodsList.innerHTML='';periods.forEach((period,index)=>{const editor=document.createElement('div');editor.className='period-editor';editor.innerHTML=`<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:10px;"><strong style="font-size:14px;">Period ${index+1}</strong><button style="background:#ff6b6b;color:white;border:none;padding:4px 8px;border-radius:4px;cursor:pointer;" onclick="deletePeriod('${selectedDay}',${index})">Delete</button></div><div class="period-editor-row"><div><label>Start Time:</label><input type="time" value="${period.start}" onchange="updatePeriodField('${selectedDay}',${index},'start',this.value)"></div><div><label>End Time:</label><input type="time" value="${period.end}" onchange="updatePeriodField('${selectedDay}',${index},'end',this.value)"></div></div><label>Subject:</label><input type="text" value="${period.subject}" onchange="updatePeriodField('${selectedDay}',${index},'subject',this.value)"><label>Type:</label><select onchange="updatePeriodField('${selectedDay}',${index},'type',this.value)"><option value="theory" ${period.type==='theory' ? 'selected':''}>Theory</option><option value="lab" ${period.type==='lab' ? 'selected':''}>Lab</option><option value="elective" ${period.type==='elective' ? 'selected':''}>Elective</option><option value="project" ${period.type==='project' ? 'selected':''}>Project</option><option value="break" ${period.type==='break' ? 'selected':''}>Break</option><option value="sp" ${period.type==='sp' ? 'selected':''}>Special</option><option value="free" ${period.type==='free' ? 'selected':''}>Free</option><option value="seminar" ${period.type==='seminar' ? 'selected':''}>Seminar</option><option value="workshop" ${period.type==='workshop' ? 'selected':''}>Workshop</option><option value="other" ${period.type==='other' ? 'selected':''}>Other</option></select>`;periodsList.appendChild(editor);});const addBtn=document.createElement('button');addBtn.className='btn-save';addBtn.style.width='100%';addBtn.textContent='+Add Period';addBtn.onclick=()=>addNewPeriod(selectedDay);periodsList.appendChild(addBtn);const saveBtn=document.createElement('button');saveBtn.className='btn-save';saveBtn.style.width='100%';saveBtn.style.marginTop='10px';saveBtn.textContent='Save Changes';saveBtn.onclick=()=>{saveTimetable();renderDay();alert('Timetable updated successfully!');};periodsList.appendChild(saveBtn);}function updatePeriodField(day,index,field,value){if(currentTimetable[day] && currentTimetable[day][index]){currentTimetable[day][index][field]=value;}}function deletePeriod(day,index){if(confirm('Delete this period?')){currentTimetable[day].splice(index,1);updateEditForm();}}function addNewPeriod(day){const newPeriod={start:'09:00',end:'10:00',subject:'New Subject',type:'theory'};currentTimetable[day].push(newPeriod);updateEditForm();}function timeToMinutes(t){const [h,m]=t.split(":").map(Number);return h*60+m;}function renderDay(){const day=days[currentDayIndex];document.getElementById("dayinfo").innerText=day;const now=new Date();const nowMin=now.getHours()*60+now.getMinutes();const container=document.getElementById("timetable");container.innerHTML="";currentTimetable[day].forEach(p=>{const active=now.getDay()-1===currentDayIndex && nowMin>=timeToMinutes(p.start)&& nowMin<=timeToMinutes(p.end);const div=document.createElement("div");div.className=`period ${active ? "active":""}`;div.innerHTML=`<div class="info"><div class="time">${p.start}– ${p.end}</div><div class="subject">${p.subject}</div></div><div class="type ${p.type}">${p.type.toUpperCase()}</div>`;container.appendChild(div);});}function prevDay(){currentDayIndex=(currentDayIndex-1+days.length)% days.length;renderDay();}function nextDay(){currentDayIndex=(currentDayIndex+1)% days.length;renderDay();}let startX=0,startY=0;document.addEventListener("touchstart",e=>{startX=e.touches[0].clientX;startY=e.touches[0].clientY;});document.addEventListener("touchend",e=>{const diffX=e.changedTouches[0].clientX-startX;const diffY=e.changedTouches[0].clientY-startY;if(Math.abs(diffX)>Math.abs(diffY)&& Math.abs(diffX)>80){if(diffX>0)prevDay();else nextDay();}});document.addEventListener('keydown',e=>{if(e.key==='ArrowLeft'){prevDay();}else if(e.key==='ArrowRight'){nextDay();}});setInterval(renderDay,60000);renderDay();const activeElement=document.querySelector('.period.active');if(activeElement){activeElement.scrollIntoView({behavior:'smooth',block:'center'});}function requestNotificationPermission(){if('Notification' in window && Notification.permission==='default'){Notification.requestPermission();}}function getNextPeriod(){const now=new Date();const currentDay=now.getDay()-1;const nowMin=now.getHours()*60+now.getMinutes();if(currentDay>=0 && currentDay<days.length){const todaysPeriods=currentTimetable[days[currentDay]];for(let period of todaysPeriods){if(timeToMinutes(period.start)>nowMin){return{day:days[currentDay],period:period,minutesUntilStart:timeToMinutes(period.start)-nowMin};}}}let nextDayIndex=(currentDay+1)% days.length;if(nextDayIndex>currentDay || nextDayIndex===0){return{day:days[nextDayIndex],period:currentTimetable[days[nextDayIndex]][0],minutesUntilStart:Infinity};}return null;}let notificationShown=false;let notificationStartTime=null;function checkAndNotify(){const next=getNextPeriod();if(!next)return;const minutesUntil=next.minutesUntilStart;if(minutesUntil<=5 && minutesUntil>4.5 && !notificationShown){if('Notification' in window && Notification.permission==='granted'){notificationShown=true;notificationStartTime=Date.now();const notification=new Notification('Next Period Starting Soon! ⏰',{body:`${next.period.subject}starts at ${next.period.start}`,icon:'assets/favicon-96x96.png',badge:'assets/favicon-96x96.png',tag:'period-notification',requireInteraction:true,data:{period:next.period,day:next.day}});notification.onclick=()=>{window.focus();notification.close();};const countdownInterval=setInterval(()=>{const elapsed=(Date.now()-notificationStartTime)/1000;const remaining=Math.max(0,300-Math.floor(elapsed));if(remaining>0){const mins=Math.floor(remaining/60);const secs=remaining % 60;const timeStr=`${mins}:${secs<10 ? '0':''}${secs}`;notification.close();new Notification('Next Period Starting Soon! ⏰',{body:`${next.period.subject}starts at ${next.period.start}\n⏱️ ${timeStr}remaining`,icon:'assets/favicon-96x96.png',badge:'assets/favicon-96x96.png',tag:'period-notification',requireInteraction:true,data:{period:next.period,day:next.day}});}else{clearInterval(countdownInterval);notificationShown=false;}},1000);}}if(minutesUntil<=0){notificationShown=false;}}setInterval(checkAndNotify,10000);requestNotificationPermission();checkAndNotify();